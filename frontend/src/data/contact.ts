import type { ContactFaqKey, ContactInfoKey } from 'types';

export const contactInfoKeys: ContactInfoKey[] = ['email', 'phone', 'address'];

export const contactFaqKeys: ContactFaqKey[] = [
  'booking',
  'checkIn',
  'pets',
  'parking',
];
